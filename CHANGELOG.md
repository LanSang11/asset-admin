# 更新记录

这里只写「这次修了什么」。服务器地址、密钥、内部文档不写进来。

## 2026-09-24

- 功能同步一批：资产导入先预览校验结果再入库；扫码页面在手机上补完；表格列显示偏好可以记住；员工/资产的关键字段改动会留下变更时间线；员工导出附带数据来源标记。
- 登录更稳：动态码第二步独立成专属步骤并使用短时效挑战；限时验收模式的时长可配置（15～480 分钟）；手机浏览器里登录按钮不再被底部工具栏遮挡；登录后跳转不再落在空白门户页。
- 安全增强：动态验证密钥改为加密存库；审计日志脱敏范围扩大到联系方式并支持嵌套结构；Nginx 部署模板补上附件目录禁执行与 Forwarded 头加固。
- 体验与助手：表单校验错误改为可读提示；只读助手可以查报修/调拨/盘点；AI 提问不再被误当成搜索关键词；用户列表支持按姓名/别名筛选；超级管理员可强制其他用户下线。

## 2026-09-14

- **换登录主密钥之后，动态验证密文也要一起换加密。** 动态验证密钥是按当时的主密钥加密存库的。只换主密钥、不重加密这一列，系统会当成「没绑定」。超级管理员用密码就能进，但进不去业务页，个人资料却显示「已启用」。正确做法：用旧主密钥解开，再用新主密钥存回去，然后用手机里原来的 6 位码重新登录。不必重新扫码。业务程序代码没有改。

## 2026-08-22

- 公开说明和功能截图整理。详见 `PROJECT-JOURNEY.md`。
